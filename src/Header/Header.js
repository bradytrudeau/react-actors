// Import React library
import React, { Component } from "react";

class Header extends Component {
    render() {
        return (
            <div>

            </div>
        );
    }
}

// Export this file to the app
export default Header;